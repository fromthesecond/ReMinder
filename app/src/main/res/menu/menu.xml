<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">

    <group android:id="@+id/search_group">
        <item android:id="@+id/plus1"
            android:title="search"
            android:orderInCategory="1"
            android:visible="false"
            app:actionViewClass="android.support.v7.widget.SearchView"
            app:showAsAction="always"/>
    </group>
    <group android:id="@+id/delete_group">
        <item android:id="@+id/delete_button"
            android:title="delete"
            android:orderInCategory="2"
            android:icon="@drawable/ic_delete_white_24dp"
            app:showAsAction="always"
            android:visible="false"/>
        <item android:id="@+id/share_button"
            android:title="share"
            android:orderInCategory="1"
            android:icon="@drawable/ic_share_white_24dp"
            app:showAsAction="always"
            android:visible="false"/>
    </group>
    <group android:id="@+id/ok_group">
        <item android:id="@+id/delete_button_create_view"
            android:title="delete"
            android:orderInCategory="2"
            android:icon="@drawable/ic_delete_white_24dp"
            app:showAsAction="always"
            android:visible="false"/>
        <item android:id="@+id/okay_button"
            android:title="Okay"
            android:orderInCategory="2"
            app:showAsAction="always"
            android:visible="false"
            android:icon="@drawable/ic_check_white_24dp"/>
    </group>
</menu>
